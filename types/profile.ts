export interface Profile {
    name: string;
    picture_url: string;
    day_points: number;
    week_points: number;
    month_points: number;
    season_points: number;
    all_time_points: number;
}